import { Typography } from "@suid/material";
import StackRowCentered from "../StackRowCentered";

const VehicleActivityHistoryNoneRow = (props) => {
  return (
    <StackRowCentered justifyContent='center'>
      <Typography>No changes.</Typography>
    </StackRowCentered>
  )
}

export default VehicleActivityHistoryNoneRow;